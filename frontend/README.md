# Frontend

React + Vite + Tailwind CSS client for College RAG Assistant. The current client includes registration, login, authenticated chat, conversation history, source citations, and session management.

## Commands

```powershell
npm install
npm run dev
npm run build
```

`npm install` and `npm run build` require Node.js and npm.
